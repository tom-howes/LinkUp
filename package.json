{
  "name": "linkup",
  "version": "1.0.0",
  "description": "LinkUp - targeted job matching and private chat. Root scripts for the backend (Express + MongoDB) and frontend (React) workspaces.",
  "private": true,
  "license": "MIT",
  "author": "Tony Zhang and Thomas Howes",
  "repository": {
    "type": "git",
    "url": "https://github.com/tom-howes/LinkUp.git"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend",
    "format:check": "npm run format:check --prefix backend && npm run format:check --prefix frontend"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
